import React from "react";
// import proptypes

// make sure that prop is a function to update state for selecting activity

const SelectActivity = props => {
	return <p>Select an activity...</p>;
};

export default SelectActivity;
